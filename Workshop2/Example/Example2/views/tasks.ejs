<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>User Profile</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<h1>My To-Do List</h1>
<ul>
    <% if (tasks.length > 0) { %>
        <% tasks.forEach(task => { %>
            <li><%= task %></li>
        <% }); %>
    <% } else { %>
        <li>You have no tasks. Great job!</li>
    <% } %>
</ul>
</body>
</html>